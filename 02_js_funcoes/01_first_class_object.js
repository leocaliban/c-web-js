// Forma literal de ciração de função
function test() { }

// Armazenar dado de função em variável (função anônima)
const valor = function () { }

// Parâmetros variáveis
function a() {
    // Acesso aos parâmetros (arguments)
    console.log(arguments);
}
a(1, 'szadd');
